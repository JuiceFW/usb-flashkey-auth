# USB FlashKey Auth

USB FlashKey Auth — это Python-модуль для простой аппаратной аутентификации с помощью USB-флешки под Windows.

Проект позволяет превратить обычную USB-флешку в физический «ключ» доступа, привязанный к конкретному устройству и (опционально) секрету. Подходит для защиты приложений, скриптов или внутренних инструментов без использования сетевых лицензий и серверов.

---

## Возможности

- 🔐 Аутентификация по USB-флешке
- 🧩 Привязка ключа к конкретной флешке (PNPDeviceID)
- 🔑 Поддержка дополнительного секрета
- 🖥 Возможность привязки секрета:
  - к Machine GUID (HWID системы)
  - к железу (меняется при апгрейде)
  - или полностью без секрета
- 👻 Скрытый файл ключа на флешке
- ⚙️ Автоматический поиск USB-накопителей

---

## Как это работает

1. На флешке создаётся скрытый файл `.usbkey`
2. В файл записывается SHA-256 хеш: `hash = sha256(PNPDeviceID + secret)`
3. При проверке библиотека:
- ищет подключённые USB-накопители
- читает `.usbkey`
- пересчитывает хеш и сравнивает

Если совпало — доступ разрешён.

---

## Требования

- Windows (используется WMI и WinAPI)
- Python 3.9+
- Пакеты:
- `wmi`
- `pywin32` (косвенно, для `winreg` и WinAPI)

---

## Установка зависимостей

```bash
pip install wmi pywin32
```

---

## Использование

Пример использования модуля приведен в файле `test.py`.

### Инициализация ключа (один раз)
```python
from flashkey import FlashKeyAuth

auth = FlashKeyAuth()
auth.init_key() # auth.check_key()
```

Во время инициализации будет предложено выбрать режим секрета:
1. Сгенерированный
2. Введённый вручную
3. HWID компьютера
4. HWID железа
5. Без секрета


### Проверка ключа
```python
from flashkey import FlashKeyAuth

auth = FlashKeyAuth(secret="ВАШ_СЕКРЕТ") # либо без указания, тогда модуль спросит через input

if auth.check_key():
    print("Доступ разрешён")
else:
    print("Ключ не найден или недействителен")
```

--- 

## Ограничения
* ❗ Работает только под Windows
* ❗ Флешка должна быть подключена напрямую (не через виртуальные устройства)
* ❗ Не предназначено для защиты профессионального реверса

---

## Возможные сценарии использования
* Запуск внутренних утилит
* Защита скриптов и админ-инструментов
* Локальная лицензия без сервера
* Физический "ключ" для приложений

---

## Безопасность

Проект **не** претендует на криптографическую защиту высокого уровня. Это аппаратный фактор доступа, а не DRM.

---

## Языки/Languages
- [Русский README](README.md)
- [English README](README-en.md)

---
